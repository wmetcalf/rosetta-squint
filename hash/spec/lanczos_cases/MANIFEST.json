{
  "cases": [
    {
      "body_format": "uint8 array of src_w*src_h, then uint8 array of dst_w*dst_h, row-major",
      "dst_size": [
        32,
        32
      ],
      "file": "lanczos_cases/downsample_64_to_32_gradient.bin",
      "header_format": "uint32 LE: src_w, src_h, dst_w, dst_h",
      "name": "downsample_64_to_32_gradient",
      "src_size": [
        64,
        64
      ]
    },
    {
      "body_format": "uint8 array of src_w*src_h, then uint8 array of dst_w*dst_h, row-major",
      "dst_size": [
        32,
        32
      ],
      "file": "lanczos_cases/upsample_16_to_32_gradient.bin",
      "header_format": "uint32 LE: src_w, src_h, dst_w, dst_h",
      "name": "upsample_16_to_32_gradient",
      "src_size": [
        16,
        16
      ]
    },
    {
      "body_format": "uint8 array of src_w*src_h, then uint8 array of dst_w*dst_h, row-major",
      "dst_size": [
        32,
        32
      ],
      "file": "lanczos_cases/identity_32_to_32_random.bin",
      "header_format": "uint32 LE: src_w, src_h, dst_w, dst_h",
      "name": "identity_32_to_32_random",
      "src_size": [
        32,
        32
      ]
    },
    {
      "body_format": "uint8 array of src_w*src_h, then uint8 array of dst_w*dst_h, row-major",
      "dst_size": [
        32,
        24
      ],
      "file": "lanczos_cases/asymmetric_64x48_to_32x24.bin",
      "header_format": "uint32 LE: src_w, src_h, dst_w, dst_h",
      "name": "asymmetric_64x48_to_32x24",
      "src_size": [
        64,
        48
      ]
    }
  ],
  "description": "Lanczos resize reference cases. Each .bin contains a (src_w, src_h, dst_w, dst_h) uint32_le header, then the src uint8 buffer (row-major, grayscale), then the dst uint8 buffer.",
  "pil_version_note": "Produced with Pillow 12.2.0 LANCZOS filter on 'L'-mode images."
}
